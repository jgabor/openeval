id: kwyfmyzdjh
artifact: todo
record:
  severity: normal
  status: open
  description: Add `examples/ci/openeval.yml` (or similar) for consumers
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: Add `examples/ci/openeval.yml` (or similar) for consumers
      severity: normal
      status: open
      order: 34
