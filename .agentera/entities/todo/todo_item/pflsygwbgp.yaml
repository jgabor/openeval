id: pflsygwbgp
artifact: todo
record:
  kind: task
  target_version: 0.0.14
  title: Allocate `external-metrics.json` inside each attempt artifact directory and pass its path to the child as `OPENEVAL_EXTERNAL_METRICS_PATH`
  requirements:
    []
  acceptance:
    []
  release_blocker: false
  severity: normal
  status: open
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[task:0.0.14] Allocate `external-metrics.json` inside each attempt artifact directory and pass its path to the child as `OPENEVAL_EXTERNAL_METRICS_PATH`"
      severity: normal
      status: open
      order: 6
