id: yecpkdfmqd
artifact: todo
record:
  severity: normal
  status: resolved
  description: Command runs baseline + skill variation + `compare` (or prints commands with `--dry-run`)
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: Command runs baseline + skill variation + `compare` (or prints commands with `--dry-run`)
      status: resolved
      order: 52
      severity: normal
