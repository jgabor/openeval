id: hqbhbeigvz
artifact: todo
record:
  kind: variation-command
  target_version: 0.0.17
  title: Support argv-safe per-variation wrappers
  requirements:
    - Add optional `Variation.Command` as a YAML sequence whose elements are literal argv entries.
    - Prepend the wrapper without a shell, interpolation, whitespace splitting, or environment expansion.
    - Retain the resolved agent identity and effective argv.
  acceptance:
    - Arguments and executable paths containing spaces reach the child unchanged.
    - An empty executable or argv entry fails before execution.
    - Scenarios without a wrapper preserve direct-agent behavior.
  release_blocker: false
  severity: normal
  status: open
  reconciliation:
    schema_version: agentera.todoReconciliation.v1
    public:
      present: true
      description: "[variation-command:0.0.17] Support argv-safe per-variation wrappers"
      severity: normal
      status: open
      order: 54
