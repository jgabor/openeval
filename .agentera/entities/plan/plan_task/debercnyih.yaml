id: debercnyih
artifact: plan
record:
  name: Diagnose OpenCode-first setup
  depends_on:
    - toxklsexpe
  status: pending
  acceptance:
    - GIVEN a user runs setup diagnosis WHEN configuration, runtime, authentication, skills, or telemetry are invalid THEN each result includes concrete remediation.
    - GIVEN only nonfatal telemetry reachability fails WHEN diagnosis finishes THEN the result is a warning rather than a fatal setup failure.
    - GIVEN automation consumes diagnosis WHEN structured output is requested THEN fields and exit status are stable and no paid model call occurs.
  plan: noradwhfpb
