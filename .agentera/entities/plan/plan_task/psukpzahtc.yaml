id: psukpzahtc
artifact: plan
record:
  name: Prove summary-span receipt and trace lookup
  depends_on:
    - scelozsjsk
  status: pending
  acceptance:
    - GIVEN the collector is ready and one bounded successful mock harness round runs WHEN score.json is inspected THEN it retains the source summary trace ID emitted to the configured OTLP endpoint.
    - GIVEN the recorded source trace ID WHEN receipt is checked THEN Jaeger's query API returns the telemetry-normalized trace ID with span name openeval.session and service openeval-agent within a fixed timeout.
    - GIVEN that received trace WHEN openeval traces <run-dir> --task <id> --round 1 runs THEN its Jaeger URL contains the same normalized trace ID.
    - GIVEN native OpenCode OTEL is enabled WHEN traces are inspected THEN native and summary traces remain separate and correlation uses openeval.trace_id.
    - GIVEN the collector is stopped WHEN the same harness path runs THEN the run still completes and retains trace and score evidence.
  plan: acgkfkknys
