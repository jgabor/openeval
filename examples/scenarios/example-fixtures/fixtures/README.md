# Maintainer Tools

This small Python package contains helpers used by a command-line maintenance tool.
It uses only the Python standard library and does not need installation. OpenEval
runs scenario-owned graders against this package after the agent finishes.
