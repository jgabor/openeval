# Maintainer Tools

This small Python package contains helpers used by a command-line maintenance tool.
It uses only the Python standard library and does not need installation. Run its
tests from this directory with:

```sh
python3 -m unittest discover -s tests
```
